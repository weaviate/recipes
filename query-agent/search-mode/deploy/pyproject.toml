[project]
name = "search-mode-server"
version = "0.1.0"
description = "Stateless HTTP service that serves Weaviate Query Agent search-mode requests"
requires-python = ">=3.10"
dependencies = [
    "weaviate-client[agents]>=4.16.0",
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.30.0",
]

[tool.uv]
package = false
