apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: search-mode-server
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: search-mode-server
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
