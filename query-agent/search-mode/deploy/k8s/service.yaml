apiVersion: v1
kind: Service
metadata:
  name: search-mode-server
  labels:
    app: search-mode-server
spec:
  type: ClusterIP
  selector:
    app: search-mode-server
  ports:
    - name: http
      port: 80
      targetPort: http
